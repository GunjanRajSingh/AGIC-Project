apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: auth-frontend
  template:
    metadata:
      labels:
        app: auth-frontend
    spec:
      containers:
      - name: auth-frontend
        image: containerregnew.azurecr.io/auth-frontend:2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        env:
        - name: REACT_APP_API_URL
          value: "http://auth-api.default.svc.cluster.local:8080"
---
apiVersion: v1
kind: Service
metadata:
  name: auth-frontend
spec:
  selector:
    app: auth-frontend
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
