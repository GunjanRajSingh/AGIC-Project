apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-frontend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-frontend
  template:
    metadata:
      labels:
        app: user-frontend
    spec:
      containers:
      - name: user-frontend
        image: containerregnew.azurecr.io/user-frontend:2
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 80
        env:
        - name: REACT_APP_API_URL
          value: "http://user-api.default.svc.cluster.local:8080"
---
apiVersion: v1
kind: Service
metadata:
  name: user-frontend
spec:
  selector:
    app: user-frontend
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
